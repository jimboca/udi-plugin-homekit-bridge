# Plugins Node Server runtime (install on the Plugins host).
# Requires Python 3.10+. PyISY comes transitively via udi_interface.
udi_interface>=3.0.31
HAP-python[QRCode]>=5.0
zeroconf>=0.132.2
markdown2
segno
